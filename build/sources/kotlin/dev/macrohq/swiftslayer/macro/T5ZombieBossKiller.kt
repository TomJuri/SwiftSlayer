package dev.macrohq.swiftslayer.macro

class T5ZombieBossKiller {
  /* val viablePositions = BlockUtil.neighbourGenerator(target!!.getStandingOnCeil(), 4).filter {
     !listOf(Blocks.air, Blocks.stained_hardened_clay, Blocks.acacia_stairs, Blocks.stone_slab2, Blocks.quartz_stairs, Blocks.wooden_slab).contains(world.getBlockState(it).block) && world.getBlockState(it.up()).block == Blocks.air && world.getBlockState(
       it.up().up()
     ).block == Blocks.air
   }*/
}